describe("CMS Bootstrap Test", () => {
    // delete me once some real tests are added to this package
    // this allows the publish scripts to run without errors
    it('lets the project build with a useless passing test', () => {
        expect(1).toEqual(1);
    });
});
